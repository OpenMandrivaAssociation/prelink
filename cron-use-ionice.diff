Index: prelink.cron
===================================================================
--- prelink.cron	(revision 9881)
+++ prelink.cron	(revision 108452)
@@ -3,6 +3,7 @@
 . /etc/sysconfig/prelink
 
 renice +19 -p $$ >/dev/null 2>&1
+ionice -n7 -p $$ >/dev/null 2>&1
 
 if [ "$PRELINKING" != yes ]; then
   if [ -f /etc/prelink.cache ]; then
